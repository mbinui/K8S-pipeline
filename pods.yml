apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: mbinui/k8s-app:tagVersion
      ports:
        - containerPort: 8080
